{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mongodb-datasource": {
    "host": "${MONGO_DB_HOST}",
    "port": "${MONGO_DB_PORT}",
    "database": "loopbackVueCommenting",
    "password": "",
    "name": "mongodb-datasource",
    "user": "root",
    "connector": "mongodb"
  }
}
